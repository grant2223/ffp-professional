/* FFP BUILD STAMP — the SINGLE source of truth for the version / cache-bust.
   Bump ONLY this one line to ship any change. The 1.8MB member-dashboard shell
   no longer needs editing just to change the build number. */
window.FFP_BUILD = '585';
